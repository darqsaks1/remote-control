export type TCoordinates = {
  x: number;
  y: number;
};

export type THttpStatus = {
  OK: number;
  BAD_RESPONSE: number;
};

export type TPrintScreenNumbers = {
  CURRENT_MOUSE_POSITION: number;
  IMAGE_FIGURE_CAPTURE: number;
};
